import { typeMeta } from '../data/demoTrip'
import { Sparkles } from 'lucide-react'
export default function ItineraryTimeline({ days=[], onSelect, personalized=[] }) {
 return <div className="space-y-6">{days.map(day=><section key={day.day}><div className="mb-3 flex items-end justify-between"><div><div className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Day {day.day}</div><h3 className="mt-1 text-lg font-semibold">{day.title}</h3></div><div className="text-sm text-slate-500">{day.date}</div></div><div className="space-y-2">{day.items.map(item=><button key={item.id} onClick={()=>onSelect?.(item)} className="group flex w-full items-center gap-4 rounded-2xl border border-[rgb(var(--c-overlay)/7%)] bg-[rgb(var(--c-overlay)/2.5%)] p-4 text-left transition hover:border-primary/25 hover:bg-primary/[.04]"><div className="w-12 shrink-0 text-xs font-medium text-slate-500">{item.start_time}</div><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-[rgb(var(--c-overlay)/5%)]">{typeMeta[item.type]?.icon || '•'}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="font-medium">{item.title}</span>{personalized.includes(item.id)&&<span title="AI-picked for your interests" className="flex shrink-0 items-center gap-1 rounded-full bg-primary/10 px-2 py-0.5 text-[10px] font-medium text-primary"><Sparkles size={10}/>Personalized</span>}</div><div className="mt-1 truncate text-xs text-slate-500">{item.location}</div></div><div className="hidden text-right sm:block"><div className="text-sm">{item.cost ? `₹${item.cost.toLocaleString('en-IN')}` : 'Free'}</div><div className="mt-1 text-xs text-success">{item.status}</div></div></button>)}</div></section>)}</div>
}
